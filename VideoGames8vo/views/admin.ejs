<%- include layouts/header.ejs%>
      <div class="container">
      <h1 class="row	justify-content-center">JUEGOS PENDIENTES POR ACEPTAR</h1>
      <% games.map(game=>{%>    
      <div class="row	justify-content-center">
      <div class="card mb-3" style="max-width: 600px;">
          <div class="row g-0">
            <div class="col-md-8">
              <div class="card-body">
              <form method="post" target="/admin" id="newGame">
                <h5 class="card-title"><%= game.titulo %></h5>
                <input type="hidden" name="id" id=<%= game.id %> value=<%= game.id %> >
                <button type="submit" class="btn btn-primary">Aceptar solicitud</button>
              </form>
              </div>
            </div>
            <div class="col-md-4">
              <img src="https://elandroidelibre.elespanol.com/wp-content/uploads/2019/05/League-of-Legends-destacada.jpg" alt="Cinque Terre" style="max-width: 250px;">
            </div>
          </div>    
      </div>
      </div>
      <% })%>
      </div>


      
      <script>
        function update(id){
          axios.put('/update', {id})
            .then(res => console.log(res))
            .catch(e => console.log(e));
        }
      </script>
      <!--<script>
          const newGame = document.querySelector('#newGame');
          const idGame = document.querySelector('#idGame');
          newGame.addEventListener('submit', (e) => {
            e.preventDefault();
            console.log(idGame.value, 'hola')
            axios.put('/update', {id: idGame.value})
            .then(res => {
            console.log(res)
            })
            .catch(e => {
            console.log(e)
            });
            // console.log(user.value, password.value);
            // axios.post('/loguear', {user: user.value, password: password.value})
            // .then(res => console.log(res))
            // .catch(e => console.log(e));
          }) 
     </script>-->
<%- include layouts/footer.ejs%>